# Real archive baseline — 2026-09-12

Measured release CLI from commit `79b4259dd755b9880b07c68aea0f2e51c31c1156` on
macOS 27.0, Mac16,8 (arm64, 24 GiB RAM), Xcode 27 beta / Swift 6.4.
Three repetitions; OS cache uncontrolled, independent corpus copying precedes timing.
The first import took 25.67 s, later imports 12.47 / 11.94 s: report that variance,
not a cold-disk claim. Original logs and the user's production index were not modified.

Corpus: **155 real JSONL files, 1,271,886,559 bytes (1.18 GiB)**.
Fingerprint: `04a9ded8e1251c37441a5000d211800f9852fbb0de542e37c45cc9745aa891e0`.
The corpus came from the previous audit selection; this is not all 1,694 current
session/archive files. Per-file independent copies avoided the selection's live hard links.

| Scenario | Wall min / median / max | CPU median | Peak RSS max | Source bytes read per run |
| --- | --- | --- | --- | --- |
| Fresh index | 11.94 / 12.47 / 25.67 s | 8.68 s | 270.92 MiB | 1,271,886,559 |
| Unchanged | 0.09 / 0.09 / 0.19 s | 0.03 s | 12.56 MiB | 0 |
| Append | 0.33 / 0.36 / 0.36 s | 0.22 s | 356.23 MiB | 357,981,808 |

Append adds **752 bytes** to one completed 357,981,056-byte source; the other 154 files
are skipped. It reads **357,981,808 bytes**, because the old prefix is verified with
SHA256. `append.reads_only_delta = false`: the strict SM-105 delta-only criterion
is not met. No recovery/verification rule was weakened to improve the benchmark.

After writers exit, logical DB + WAL + SHM size is **4,894,720 bytes (4.67 MiB)**:
DB 4,861,952, WAL 0, SHM 32,768. This is not peak disk allocation or peak WAL.

## Idle and correctness

- Watch alone, 30.0105 s: CPU delta below the 0.01 s measurement resolution.
- Watch + one observer, 30.0114 s: watch delta below resolution; observer 0.01 s,
  approximately **0.033% of one CPU core**. Quantization is itself about 0.033 percentage
  points over this interval. These are short samples, not a long-term energy estimate.
- No output updates occurred during idle intervals; processes stopped successfully.
- Fresh Python reference audit on the same copied corpus matched **6,340 requests**
  and all six token totals for `[2026-09-05T05:27:20Z, 2026-09-12T05:27:20Z)`.
  Unknown cache requests: 0. Append was outside that window; its report stayed equal.
- The complete incremental report after append matched an independent full rebuild,
  including sessions and diagnostics.

## Reproduction and evidence

See [methodology and command](README.md). The CLI binary SHA256 was
`eb9ac9642a78ed47c639e8badaacafc6aeed4bb7602db966c260d1a9718a72e2`.
Local detailed evidence: `.build/sm105-real-baseline/baseline.json`, timing logs,
`private-manifest.json`, and the reference audit output. These contain local information
and are excluded from Git. The independent 1.2 GiB corpus copy was removed after
successful measurement when the machine ran low on disk; source logs and evidence remain.
A future run captures a new corpus fingerprint. Use `--keep-corpus` on the harness
when an exact retained input is required and sufficient space is available.

The benchmark initially completed successfully. A subsequent quality gate encountered
`ENOSPC`; after cleanup, the harness gained a disk-space preflight and automatic copy
cleanup on success. These controls do not change the measured Swift binary or samples.

SM-105 measurement work is delivered; delta-only append remains unimplemented and
must not be inferred from the zero-byte unchanged result.
